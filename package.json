{
  "name": "@codeine-dev/ioc",
  "version": "0.0.0-beta1",
  "description": "A simple Inversion-of-Control library",
  "main": "lib/index.js",
  "scripts": {
    "test": "ts-mocha -p tsconfig.test.json 'src/**/*.spec.ts'",
    "test:watch": "npm test -- --watch --watch-files 'src/**/*.ts'",
    "prepack": "npm run build",
    "build": "tsc -p tsconfig.json"
  },
  "files": [
    "lib"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "@types/node": "^17.0.35",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.7.2"
  }
}
